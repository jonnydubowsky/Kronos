# Kronos Financial Time Series Prediction - Environment Configuration Template
# Copy this file to .env and fill in your actual values

# ============================================================================
# Optional: Experiment Tracking (Comet.ml)
# ============================================================================
# If you want to use Comet.ml for experiment tracking during fine-tuning
# Sign up at: https://www.comet.com/
COMET_API_KEY=your_comet_api_key_here
COMET_WORKSPACE=your_comet_workspace_name
COMET_PROJECT_NAME=Kronos-Finetune

# ============================================================================
# Model Paths (Optional - defaults to Hugging Face Hub)
# ============================================================================
# Leave these blank to download from Hugging Face automatically
# Or specify local paths if you've downloaded models locally

# Pretrained Models (downloaded from Hugging Face)
PRETRAINED_TOKENIZER_PATH=NeoQuasar/Kronos-Tokenizer-base
PRETRAINED_PREDICTOR_PATH=NeoQuasar/Kronos-small

# Fine-tuned Models (saved locally after training)
FINETUNED_TOKENIZER_PATH=./outputs/models/finetune_tokenizer_demo/checkpoints/best_model
FINETUNED_PREDICTOR_PATH=./outputs/models/finetune_predictor_demo/checkpoints/best_model

# ============================================================================
# Data Paths (for fine-tuning)
# ============================================================================
# Path to Qlib data (only needed for Chinese A-share market fine-tuning)
# Install qlib data following: https://github.com/microsoft/qlib
QLIB_DATA_PATH=~/.qlib/qlib_data/cn_data

# Processed dataset output path
DATASET_PATH=./data/processed_datasets

# ============================================================================
# Output Paths
# ============================================================================
# Base directory for saving model checkpoints
SAVE_PATH=./outputs/models

# Backtesting results directory
BACKTEST_RESULT_PATH=./outputs/backtest_results

# ============================================================================
# Compute Settings
# ============================================================================
# Device to use for inference: cpu, cuda:0, cuda:1, mps (for Apple Silicon)
DEVICE=cpu

# Number of GPUs for fine-tuning (if using multi-GPU training)
NUM_GPUS=1

# ============================================================================
# Hugging Face Hub (Optional)
# ============================================================================
# Only needed if you want to upload your fine-tuned models to Hugging Face
# Get your token from: https://huggingface.co/settings/tokens
HUGGINGFACE_TOKEN=your_huggingface_token_here

# ============================================================================
# Notes:
# ============================================================================
# 1. For basic inference/prediction: NO API keys required!
#    - Models download automatically from Hugging Face Hub
#    - Just run the examples with default settings
#
# 2. For fine-tuning:
#    - COMET_API_KEY: Optional, only if you want experiment tracking
#    - QLIB_DATA_PATH: Required if fine-tuning on Chinese A-share data
#
# 3. For custom data:
#    - Place your CSV files in ./data/ directory
#    - Required columns: open, high, low, close
#    - Optional columns: volume, amount, timestamps
#
# 4. Device selection:
#    - MacOS with Apple Silicon: use "mps" for GPU acceleration
#    - NVIDIA GPU: use "cuda:0" (or cuda:1, cuda:2, etc.)
#    - CPU only: use "cpu"
